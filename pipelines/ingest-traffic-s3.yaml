apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: ingest-traffic-s3-
spec:
  serviceAccountName: workflow
  entrypoint: ingest-traffic-s3
  templates:
    - name: ingest-traffic-s3
      dag:
        tasks:
          - name: ingest
            template: ingest
          - name: validate
            template: validate
            dependencies: [ingest]
            continueOn:
              failed: true  
            arguments:
              parameters:
                - name: object_key
                  value: "{{tasks.ingest.outputs.parameters.object_key}}"
          - name: notify
            template: notify
            dependencies: [validate]
            when: "{{tasks.validate.status}} == Failed"
    
    - name: ingest
      outputs:
        parameters:
          - name: object_key
            valueFrom:
              path: /tmp/object_key.txt
      container:
        env:
          - name: CENTER_ID
            value: "EUR_MAD_001"
          - name: RAW_BUCKET
            value: "data-platform-dev-raw-811430801421"
          - name: SIMULATION_API_URL
            value: "http://simulation-api:8000"
          - name: AWS_ACCESS_KEY_ID
            valueFrom:
              secretKeyRef:
                name: aws-credentials
                key: AWS_ACCESS_KEY_ID
          - name: AWS_SECRET_ACCESS_KEY
            valueFrom:
              secretKeyRef:
                name: aws-credentials
                key: AWS_SECRET_ACCESS_KEY
        image: python-ingestor:local
        imagePullPolicy: Never
        command: [python]
        args: [ingest.py]
      
    - name: validate
      inputs:
        parameters:
          - name: object_key
      container: 
        env:
          - name: OBJECT_KEY
            value: "{{inputs.parameters.object_key}}"
          - name: BUCKET
            value: "data-platform-dev-raw-811430801421"
          - name: EXPECTED_RECORDS
            value: "24"
          - name: AWS_ACCESS_KEY_ID
            valueFrom:
              secretKeyRef:
                name: aws-credentials
                key: AWS_ACCESS_KEY_ID
          - name: AWS_SECRET_ACCESS_KEY
            valueFrom:
              secretKeyRef:
                name: aws-credentials
                key: AWS_SECRET_ACCESS_KEY
        image: data-quality:local
        imagePullPolicy: Never
        command: [python]
        args: [validate.py]
    - name: notify
      container:
        image: alpine:3.18
        command: [echo]
        args: ["validation failed — check logs"]
